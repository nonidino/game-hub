# Copy to .env.local and fill in. These are PUBLIC, RLS-guarded values — safe to ship.
# (Secret keys for the AI/voice features live in Supabase function secrets, NOT here.)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
